export interface Breakpoint {
  name: string;
  minWidth: number;
  maxWidth: number;
  label: string;
  color: string;
  icon: string;
}

export interface LayoutMode {
  id: string;
  label: string;
  description: string;
}
